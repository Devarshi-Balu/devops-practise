# rename this file as ansible.cfg when using in a server

[defaults]
host_key_checking = False

[ssh_connection]
# Redirects known_hosts to /dev/null so no keys are saved or checked
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no
